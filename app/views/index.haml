#peeps-container
  - if maker_signed_in 
    = partial :add_peep
  %ul{id: 'peeps'}
  - @peeps.each do |peep|
    - @peep_maker = peep.maker
    =partial :list_peeps, locals: {peep: peep, peep_maker: @peep_maker}